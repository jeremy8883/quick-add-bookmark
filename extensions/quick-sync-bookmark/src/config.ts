// Public Dropbox app key. Safe to ship: with PKCE there is no client secret.
// Users can override via the options page (advanced) if they want to point
// the extension at a fork-owned Dropbox app.
export const DEFAULT_DROPBOX_APP_KEY = "vx7ivzr2d76qivm";
